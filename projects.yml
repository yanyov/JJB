- project:
    name: my_projects
    jobs:
      - '{name}_job':
            name: free_space
            job_description: 'Check free space'
            command: 'df -hT'
      - '{name}_job':
            job_name: check_running_processes
            job_description: 'Check running processes'
            command: ps -eLf
